{
	"name": "Base Dev Container",
	"dockerComposeFile": "docker-compose.yml",
	"service": "dev",
	"customizations": {
		"vscode": {
			"settings": {},
			"extensions": [
				"ms-python.python",
				"ms-azuretools.vscode-docker",
				"amazonwebservices.aws-toolkit-vscode"
			]
		}
	},
	"forwardPorts": [3000],
	"postCreateCommand": "if [ -f /workspaces/$(basename $(pwd))/package.json ]; then npm install; fi && for req in /workspaces/$(basename $(pwd))/requirements*.txt; do [ -f \"$req\" ] && pip install -r \"$req\"; done"
}

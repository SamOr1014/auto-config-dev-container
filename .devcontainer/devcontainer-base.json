{
	"name": "Base Dev Container",
	"dockerComposeFile": "./docker-compose.yml",
	"service": "dev",
	"workspaceFolder": "/workspaces/app",
	"customizations": {
		"vscode": {
			"settings": {},
			"extensions": [
				"ms-python.python",
				"ms-azuretools.vscode-docker",
				"amazonwebservices.aws-toolkit-vscode"
			]
		}
	},
	"forwardPorts": [3000],
	"postCreateCommand": "if [ -f /workspaces/app/package.json ]; then npm install; fi && for req in /workspaces/app/requirements*.txt; do [ -f \"$req\" ] && pip install -r \"$req\"; done"
}
